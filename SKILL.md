---
name: project-vault-rules
description: 项目管理规则协议（协议型 Skill，不产出内容）。约束"项目管理资料怎么落盘"：新建项目登记、记录决策 ADR、提交评审、更新进度状态、查阅项目状态、复盘归档时使用。触发时先读本 SKILL.md，再读 STANDARDS.md，按环节夹与模板执行。
---

# 项目管理规则协议

## 定位

- **协议型 Skill**：不生成任何业务内容，只约束"项目管理资料如何落盘与更新"。
- 适用于：新建项目登记、记录决策（ADR）、提交评审、更新进度状态、查阅项目进展、复盘归档。
- 真实项目资料存放在私人总仓（`TEXXXXTURE/project-vault`，私有）；本仓库是规则开源版。

## 执行协议（每次命中按此顺序）

1. **读规则**：先读 `STANDARDS.md`（当前定稿 v2），确认环节夹结构、状态、编号、ADR 规则。
2. **定位**：按 `00-index → 10-projects/<项目名>/<环节夹>` 路径定位；不存在的先建夹子。
3. **落盘**：用 `TEMPLATES/` 对应模板写文件（PRD 必须含「项目理念与设计方向」开篇章节）；过程性内容（备选、推演、否决、关键取舍、未决问题）写对应环节夹的 `过程记录.md`（STANDARDS §14，只记影响结果的过程，不记流水账）。
4. **更新状态**：文件 frontmatter `stage` / `status` 同步；索引表「当前环节」列同步。
5. **决策引用理念**：新增 ADR 时，理由必须引用 PRD 理念条目（依理念 §x）。

## 评审约定（STANDARDS §4）

单人版评审 = **无记忆 Agent**（不带本项目拆解 / 需求 / 方案上下文的 Agent）评审方案；
输出入 `03-评审意见/`，**三段式**：一句话结论 + 每条意见（看到了什么 → 担心什么 → 为什么这么判断 → 建议怎么改）+ 处置追踪列；
采纳与否由 owner 决定，处置记入 `决策/` 或直接修订，不留悬空意见。

## 边界

- 代码、成品、内容产物一律留在产品仓，总仓只记 `origin` 指针（STANDARDS §11）。
- 产品仓细节变更不回写总仓摘要；仅 FR/NFR 状态、决策、评审、问题变化需要更新。
