---
type: standards
id: standards.project-vault
name: 项目管理总仓管理标准
status: active
updated: 2026-09-25
owner: A
origin: 单人版项目管理流程推演（现实步骤→多人版→单人版，2026-09-24 逐点确认定稿；2026-09-25 v2.1 增补过程记录与评审三段式）
---

# project-vault · 管理标准（STANDARDS）v2

> 效力：本文件是总仓的**正式规则**，README 是对外简介；两处冲突时以本文件为准。
> 适用：本仓全部文件。新增项目 / 新增文件 / 决策变更必须遵守；存量项目按 §12 迁移。

---

## 0. 设计逻辑（规则为什么长这样）

本规则由**单人版项目管理流程**推演而来，不是凭空定的命名/嵌套规则：

1. **现实步骤**（立项 / 需求 / 拆解排期 / 方案设计 / 方案评审 / 执行 / 测试验证 / 修复回归 / 进度汇报 / 变更管理 / 验收交付 / 复盘归档）→ 合并同类得**多人版**；
2. 多人版中"为协作而存在"的环节（进度汇报、变更审批、评审团）→ 删减/降级，得**单人版**；
3. **单人版 = 5 个阶段环节 + 2 个贯穿环节**，每个环节都有独立推进作用，环节即文件夹，夹内即产物。

> 删减去向：进度汇报 → 索引状态字段承担；变更管理 → 并入决策环节（变更 = 新 ADR）；评审团 → 无记忆 Agent 评审（§4）。

## 1. 目录结构（命名 B · 产物式）

每个项目固定结构：

```
10-projects/<项目名>/
├── 01-需求与PRD/    PRD.md                  ← 理念 + 需求 + 验收（FR/NFR）
├── 02-计划与方案/   计划与方案.md            ← 怎么做、顺序、里程碑
├── 03-评审意见/     YYYY-MM-DD-主题.md       ← 无记忆 Agent 外部评审
├── 04-执行状态/     状态记录.md              ← 产物留产品仓，这里记进度指针
├── 05-验证记录/     测试记录.md · 问题清单.md · 自检报告.md
├── 06-复盘/         复盘.md                 ← 教训、归档
├── 决策/            ADR-0001-xxx.md         ← 贯穿：关键选择留痕
└── 问题/            debug-log.md            ← 贯穿：踩坑记录
```

夹子名稳定不变；"项目当前推进到哪个环节"由索引「当前环节」列 + 各文件 frontmatter 承载（§6）。

## 2. 环节职责与推进作用

| 环节 | 推进作用 | 产物 | 完成标志 |
|---|---|---|---|
| 01-需求与PRD | 明确做什么 + 做到什么算好 | PRD.md | PRD 定稿（理念 + FR/NFR 表齐） |
| 02-计划与方案 | 执行有路线图，不边做边想 | 计划与方案.md | 拆解、排期、方案齐 |
| 03-评审意见 | 用"新眼睛"挑出上下文盲区 | 评审意见 | 意见收敛（采纳与否已处置） |
| 04-执行状态 | 产出推进可见 | 状态记录.md | 产物交付（留产品仓） |
| 05-验证记录 | 暴露做错 / 漏掉的问题 | 测试记录 / 问题清单 | 验收项逐项通过或已挂起 |
| 06-复盘 | 教训沉淀，下个项目不踩同一坑 | 复盘.md | 归档（项目可移入 90-archive） |
| 决策（贯穿） | 关键选择留痕，变更不覆盖 | ADR | 每个决策一条，状态机完整 |
| 问题（贯穿） | 踩过的坑可查、可复用答案 | debug-log.md | 条目带日期、根因、修复 |

## 3. 理念（并入 PRD 开篇）

- 理念 = 项目的**方向纲领**：产品是什么、为谁、什么优先（决策准则）。它指导整个项目设计方向，写在 `01-需求与PRD/PRD.md` 开篇章节「项目理念与设计方向」。
- 需求取舍、方案选择、评审处置都以理念为仲裁标准；**ADR 决策必须引用理念条目作为依据**（如"依理念 §服务端优先"）。
- 理念变更 = 一次决策（新 ADR），不静默修改。

## 4. 评审机制（单人版的外部眼睛）

- 单人没有评审团，评审由**一个不带项目上下文的 Agent** 完成：它不参与本项目的拆解、需求、方案制定，模拟"第一次看到这个项目的外部专家"。
- 评审对象：`02-计划与方案` 的方案（规划后、执行前）；需要时也可评审 PRD 或最终产物。
- 输出：评审记录按日期命名入 `03-评审意见/`，**三段式结构**：
  1. **一句话结论**：整体判断 + 最该先改什么（人话，不堆术语）；
  2. **每条意见**：含「看到了什么 → 担心什么 → 为什么这么判断 → 建议怎么改」，按重要性排序，区分「必须改（阻塞）/ 建议改（优化）」；
  3. **处置追踪列**：每条意见的最终状态（采纳 / 不采纳 / 修订于哪里）。
- 意见处置：采纳 → 修订方案；不采纳 → 在处置追踪列写明理由；涉及方向性选择 → 记入决策（ADR）。**不留悬空意见**。

## 5. Frontmatter 信息卡

登记类文件（PRD / 计划方案 / 评审 / 状态 / 验证 / 复盘 / ADR / debug）**必须**以 YAML frontmatter 开头：

```yaml
---
type: prd              # prd | plan | process | review | status | verify | retro | adr | debug | standards | index
id: prd.workbench      # 唯一编号，见 §7
name: AI Agent 工作台系统   # 文档标题
status: active         # 文件级状态，见 §6
updated: 2026-09-24    # 最近实质更新日期
owner: A               # 负责人
origin: <原件路径/上游文档>  # 来源
stage: 01             # 当前推进到的环节编号（00-99），承载"推进状态"
---
```

ADR 附加：`decided` / `supersedes` / `superseded-by`；ADR 的 `status` 用决策状态机。

## 6. 状态

- **项目级**（索引 + frontmatter 共用）：`active`（🟢 进行中）/ `iterating`（🔵 迭代中）/ `paused`（⚪ 暂停）/ `done`（⚫ 已完结 → 移 `90-archive/`）。
- **推进状态**：`stage` 字段（00-06 + 决策/问题），反映当前推进到的环节；索引「当前环节」列同步。
- **ADR 决策状态机**：`proposed` → `accepted` / `rejected` → `superseded`。

## 7. 编号规则

| 对象 | 格式 | 示例 |
|---|---|---|
| 文件 id | `<type>.<项目名>[.<序号>]` | `prd.workbench`、`adr.workbench.0001` |
| PRD 需求 | `FR-xx` / `NFR-xx`（项目内递增） | `FR-01`、`NFR-03` |
| ADR | `0001` 起，项目内递增 | `ADR-0001` |
| 评审文件 | `YYYY-MM-DD-主题.md` | `2026-09-24-工作台方案评审.md` |

编号一经分配不回收；文件删除在 `问题/debug-log.md` 记一条。

## 8. ADR（决策记录）规则

1. **决策永不覆盖**：变更 = 新增一条 ADR + 旧记录标 `superseded-by`，历史完整可溯。
2. **四段式结构**：`# ADR-0001 · <一句话决策>` → 上下文 → 决策 → 理由 → 后果；理由中引用理念条目。
3. 一句话标题必须能独立读懂。

## 9. 索引规则

`00-index/index.md` 每项目一行，字段固定：

```
| 项目 | 状态 | 当前环节 | 一句话 | 产品仓位置 | 总仓目录 | 最近更新 |
```

- 新增项目必须：建目录 + 各环节夹 + 补索引行，一次完成。
- 项目状态 / 环节推进 / 登记文件实质更新时，同步更新索引行。

## 10. 接入流程（新项目，五分钟）

1. 建 `10-projects/<项目名>/` + 上述 8 个夹子（空夹放 README 占位说明）。
2. 写 `01-需求与PRD/PRD.md`：理念开篇 + 定位 + 现状 + FR/NFR 表 + 待办构想 + 下一步。
3. `00-index/index.md` 补行。
4. commit（`chore(vault): 接入项目 <项目名>`）。

## 11. 与产品仓边界

- 本仓只存**管理资料**（PRD、计划、评审、状态、验证、复盘、决策、问题）。
- 完整 PRD 原件、代码、成品、调研原文 → 产品仓；本仓登记 `origin` 指针，**不做镜像或摘要副本**。
- 产品仓细节变更不回写总仓；仅 FR/NFR 状态、决策、评审、问题变化需要更新。

## 12. 存量项目迁移

已按旧结构（01-prd / 02-ideas / 03-reviews / 04-decisions / 05-debug-log）登记的项目，按 §1 迁移：

- `01-prd.md` + `02-ideas.md` → 合并为 `01-需求与PRD/PRD.md`（**核心理念提炼为开篇「项目理念与设计方向」**，待办构想收为「待办与构想」章节）；
- `03-reviews/` → `03-评审意见/`；`04-decisions/` → `决策/`（文件名不变，补 frontmatter）；
- `05-debug-log.md` → `问题/debug-log.md`；
- 迁移只增改格式与位置，**历史内容原样保留**。

## 13. 变更本规则

- 修改本文件 = 新增一条 ADR（`adr.project-vault.xxxx`）+ 本文件 `updated` 更新。
- 本规则不自我覆盖，保持可回溯。

## 14. 过程记录（产物与过程双轨）

- 每个环节夹内配一份 `过程记录.md`，与产物文件并存：**产物 = 定稿快照，过程 = 怎么走到这一步**。
- 过程记录只记**影响结果的过程**：备选方案与推演、否决理由、关键取舍、未决问题、讨论脉络；**不记流水账**（日期流水、琐碎对话一律不收）。
- 适用：
  - 01-需求与PRD：需求讨论脉络、理念取舍
  - 02-计划与方案：备选方案、排期推演、否决理由
  - 03-评审意见：评审推理已并入评审记录三段式（§4），不另起过程文件
  - 04-执行状态：执行中的调整与原因
  - 05-验证记录：验证中发现的问题与处理过程
  - 06-复盘：复盘依据的过程材料
- 过程记录不代替 ADR：影响方向的选择仍按 §8 记决策（ADR），过程记录保留**决策前的推演底稿**。
- 过程记录按时间顺序追加，不覆盖；`updated` 随最新追加更新。
